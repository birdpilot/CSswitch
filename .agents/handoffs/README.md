# 临时 handoff 规则

本目录只放短期任务交接。除本说明外的内容由 Git 忽略，不是项目权威来源。

handoff 只应记录：

- 创建 / 更新时间以及失效或删除条件；
- 当前 worktree、分支和任务范围；
- 需要保护的脏 worktree；
- 已完成 checkpoint、下一步和未完成验证；
- 明确禁止项，例如不 push、不 release、不改产品代码。

不要复制架构、发布历史、稳定规则或长期已知问题。长期事实应链接到 `.agents/context/` 与 `docs/`；任务结束后删除临时 handoff，或将耐久事实提升到正确位置。
